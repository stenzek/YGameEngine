set(HEADER_FILES
)

set(SOURCE_FILES
)

#include_directories(${ENGINE_BASE_DIRECTORY})
#add_library(EngineMain STATIC ${HEADER_FILES} ${SOURCE_FILES})
#target_link_libraries(EngineMain EngineCore bullet)

